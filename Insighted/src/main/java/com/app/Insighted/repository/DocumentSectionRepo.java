package com.app.Insighted.repository;

import com.app.Insighted.model.DocumentSection;
import org.springframework.data.jpa.repository.JpaRepository;

public interface DocumentSectionRepo extends JpaRepository<DocumentSection, Long> {
}
